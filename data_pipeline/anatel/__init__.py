"""ANATEL dataset-level tooling.

This package contains helpers that operate on dataset-level ANATEL outputs
(e.g., `acesso_fixo`) that do not naturally fit into the point-measurement
`MeasurementSchema` used by the medallion pipeline.
"""
